# Copy to .env — use the SAME values as apps/web/.env.local (never commit .env)

# Web API base (run: pnpm --filter web dev locally, or production URL)
EXPO_PUBLIC_APP_URL=http://localhost:3000

# Supabase (anon key only — from Supabase dashboard → API)
EXPO_PUBLIC_SUPABASE_URL=https://zstnygokvxrrszvkfejs.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Sanity (read-only; sermons/blog still load via web API)
EXPO_PUBLIC_SANITY_PROJECT_ID=7hl877lg
EXPO_PUBLIC_SANITY_DATASET=production
